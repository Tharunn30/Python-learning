#It is the collection of unordered items.
#Each element in the set must be unqiue & immutable. 

collection = {1, 2, 3, 4, "hello", "world", "world"} #it ignores duplicate values

print(collection)
print(type(collection))
print(len(collection))

collection1 = {} #empty dictionary
collection2 = set() #empty set

